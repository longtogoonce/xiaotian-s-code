#include <stdio.h>
double fact (int n); 
int main()
{
    int m,n,c;
    scanf("%d %d",&m,&n);
    c=fact(n)/(fact(m)*fact(n-m));
    printf("%d",c);
    return 0;
}
double fact (int n)
{
    int i;
    double sum=1.0;
    for(i=1;i<=n;i++)
    {
        sum*=i;
    }
    return sum;
}